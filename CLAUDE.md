# 첫배포 (vibe-landing) — 작업 규칙

코딩을 전혀 모르는 **중장년·시니어**에게 "바이브코딩"을 가르치는 온라인 강의 판매 사이트.
사업자: 멜라빈 스튜디오 · 개업일 2026-10-01 · 운영 도메인 `firstdeploy.kr`

작업 브랜치는 **`claude/phase-1-basic-setup-1o3ztm`**. `main`은 아직 옛 정적 페이지다.

---

## 🔑 기본 작업 방식 — 묻지 말고 끝까지 진행한다

**사장님은 자동화가 도는 동안 다른 일을 본다.**
화면을 지켜보며 승인을 기다려야 한다면 자동화의 의미가 없다.

### 중간 승인을 요구하지 않는다

다음 질문을 반복하지 않는다:

- ❌ "이대로 진행할까요?"
- ❌ "다음 단계로 갈까요?"
- ❌ "수정해도 될까요?"

### 물어보지 않고 알아서 하는 일

파일 생성·수정 · 코드 작성 · 문서 정리 · 설정 적용 · 테스트 ·
오류 수정 · **여러 단계의 연속 작업** · 반복되는 확인

작업 도중 문제가 생겨도 **안전하게 되돌릴 수 있는 범위라면 먼저 스스로 고쳐 본다.**
막혔다고 바로 부르지 않는다.

### 반드시 사장님이 직접 해야 하는 일 (여기서만 부른다)

| 무엇 | 왜 |
|---|---|
| 비밀키 · API Key 입력 | 값을 대신 다루지 않는다 |
| 로그인 · 본인인증 | 계정 주인만 할 수 있다 |
| 결제 승인 | 돈이 나간다 |
| 계정 권한상 반드시 눌러야 하는 보안 확인 | 대신 누를 수 없다 |
| **삭제 · 결제 · 배포처럼 되돌리기 어려운 최종 결정** | 되돌릴 수 없다 |

여기 해당하면 **필요한 것만 한 번에 모아서** 알려 준다. 한 단계씩 부르지 않는다.

### 그래도 남는 승인 창

Claude Code 자체의 권한 승인 창은 내가 없앨 수 없다.
자주 쓰는 안전한 명령은 `.claude/settings.json`에 미리 허용해 두었다.
새로 걸리는 게 생기면 **묻지 말고 거기에 추가한다.**

---

## 🚫 절대 하지 않는 것

- `.env*` 커밋 (`.env.example`만 예외)
- 서버 전용 키에 `NEXT_PUBLIC_` 접두사
- RLS 끄기
- `lessons.vimeo_id`를 클라이언트 번들에 노출
- 결제 금액을 클라이언트에서 받은 값으로 신뢰 — **서버에서 DB를 다시 읽는다**
- 비밀값 출력 — 모양이나 앞글자만 말한다
- **허락 없이 `main`에 푸시**

---

## 이 프로젝트에서 반복해 배운 것

**바깥 서비스의 화면은 기억이 아니라 그날 화면이 정답이다.**
Resend의 SPF를 기억으로 TXT라고 안내했다가 틀렸다. 실제 화면은 CNAME 2개였다.
버튼 이름·메뉴 이름을 기억으로 채우지 않는다. 모르면 "화면에 뭐가 보이는지" 묻는다.

**화면의 성공은 증거가 아니다.**
로그인 시험 3번이 모두 "성공처럼 보이는 실패"였다. 세션이 남아 있었고, 주소에 오타가
있었고, 브라우저 자동완성이 주소를 바꿔치기했다. **DB 한 줄로 확인한다.**

**OBS 믹서 막대가 움직여도 녹화 파일은 무음일 수 있다.**
받는 것과 녹화로 보내는 것은 별개다. 그래서 "재생해서 확인"까지가 시험이다.

---

## 문서 지도

| 파일 | 쓰임 |
|---|---|
| `docs/RESUME_첫배포.md` | **여기부터 읽는다.** 현재 상태와 다음 할 일 |
| `docs/plans/PLAN_첫배포.md` | 7단계 계획과 Quality Gate |
| `docs/BRIEF_첫배포.md` | 무엇을 왜 만드는가 |
| `docs/course/README.md` | 강의 전체 구성 · 공통 원칙 |
| `docs/course/filming-map.md` | **강사용 촬영 전체 지도** (8강 한눈에) |
| `docs/course/recording-checklist.md` | 촬영 준비 (모든 강 공통) |
| `docs/course/lesson-0N-script.md` | 강별 대본 |

**강의 제목·요약·길이의 원본은 DB `public.lessons`다.** 문서와 어긋나면 DB가 맞다.

---

## 손님에게 이미 한 약속 (깨면 안 된다)

랜딩 FAQ와 1강은 **결제 전에 손님이 읽는다.** 여기 적힌 것은 강의가 지켜야 한다.

- 🔴 **Claude 유료 구독이 필요하다** — 무료 계정으로는 안 된다.
  1강 조각 5와 2강 맨 앞에서 말한다. **금액은 영상에 넣지 않는다** (요금은 바뀐다)
- 🔴 **7강에서 무료 주소로 배포가 끝나야 한다** — 유료 도메인 없이
- 🔴 **8강 도메인은 안 사도 강의를 완주할 수 있다**
- 후기는 전부 예시다 — `is_sample` 배지가 붙어 있다. 오픈 전 교체하거나 내린다

---

## 커밋

- 브랜치: `claude/phase-1-basic-setup-1o3ztm`
- 푸시: `git push -u origin <branch>` · 네트워크 실패만 2s→4s→8s→16s 재시도
- 커밋 메시지는 **무엇을 왜 바꿨는지** 한국어로. 모델 이름은 넣지 않는다
- 끝에 붙인다:

```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```
